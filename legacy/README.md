# Legacy CLI Tool

This folder contains the original CLI bulk downloader (`bulk_downloader.js`)
that predates the SaaS platform. It is preserved here for reference while the
multi-tenant web app is being built. It is NOT part of the Next.js build.

The web platform's equivalent functionality is implemented in
`src/services/attendance-downloader.ts` (Task 9.2).
